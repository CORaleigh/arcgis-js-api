import WidgetBase from '../widget-core/WidgetBase';
import { WNode, SupportedClassName } from '../widget-core/interfaces';
import { LinkProperties } from './interfaces';
export interface ActiveLinkProperties extends LinkProperties {
    activeClasses: SupportedClassName[];
}
export declare class ActiveLink extends WidgetBase<ActiveLinkProperties> {
    private _outletHandle;
    private _renderLink(isActive?);
    protected _onOutletPropertyChange(previous: ActiveLinkProperties, current: ActiveLinkProperties): void;
    protected render(): WNode;
}
export default ActiveLink;
