import { DNode } from '../widget-core/interfaces';
import { WidgetBase } from '../widget-core/WidgetBase';
import { MatchDetails } from './interfaces';
export interface OutletProperties {
    renderer: (matchDetails: MatchDetails) => DNode | DNode[];
    id: string;
    routerKey?: string;
}
export declare class Outlet extends WidgetBase<OutletProperties> {
    private _handle;
    protected onRouterKeyChange(current: OutletProperties, next: OutletProperties): void;
    protected onAttach(): void;
    protected render(): DNode | DNode[];
}
export default Outlet;
